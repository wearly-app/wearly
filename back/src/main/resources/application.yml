spring:
  application:
    name: wearly
  profiles:
    active: dev
  jpa:
    properties:
      hibernate:
        format_sql: true


jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: 604800000